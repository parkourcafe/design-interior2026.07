> Brand-only successor of the corresponding ARCHIDOM document. Product scope and architecture are unchanged except where a later approved addendum explicitly says otherwise.
> Current public brand: RemHaOS. Russian pronunciation: РемХаос. Primary host: https://remhaos.com.

# REMHAOS — PRODUCT & ARCHITECTURE DECISION LOG v1

**Дата freeze:** 27.07.2026  
**Правило:** решение не переоткрывается без новых фактов и OWNER DECISION или утверждённого Addendum.

> **Предупреждение для rebase (11.08.2026).** Этот файл — самое конфликтное
> место репозитория: каждая ветка с решением дописывает строку в конец таблицы.
> Открытый PR **#77** (`claude/m3-owner-decisions-files-conflicts`) был создан,
> когда последней записью была DEC-024. При его перебазировании конфликт в
> таблице разрешается **сложением обеих сторон**, а не выбором одной:
> **DEC-025 … DEC-037 обязаны уцелеть все**. Потеря любой из них тихо
> откатывает подписанное решение — открытие M4, guardrail-механизм грантов,
> воркерный этап, Telegram Bridge, production-выключатель V1 Impact или его
> коррекцию, — и заметить это будет нечем, потому что код останется на месте,
> а основание исчезнет.

| ID | Решение | Статус | Основание | Последствие |
|---|---|---|---|---|
| DEC-001 | Module 5 не создаётся | LOCKED | Charter v0.5 §5 | Studio Intelligence остаётся shared layer/package |
| DEC-002 | Одна Project Memory для M1–M4 | LOCKED | Charter §5, Architecture | Нет отдельных правд модулей |
| DEC-003 | Studio Intelligence не доменный модуль | LOCKED | Addendum A1 | Только коммерческая упаковка/settings |
| DEC-004 | ProjectFact — надтип | LOCKED | A1 §3 | fact_type: requirement/constraint/assumption/open_question |
| DEC-005 | ProposedChange удалён | LOCKED | Charter Change Control | Используются ChangeRequest и ChangeOrder |
| DEC-006 | MCP — адаптер | LOCKED | Architecture | Integration Gateway не зависит от MCP |
| DEC-007 | Workflow Engine раньше Builder | LOCKED | Architecture | Sprint 1 фиксированные templates |
| DEC-008 | AI не изменяет approved truth молча | LOCKED | Charter safety | Proposed outputs + human gate + version |
| DEC-009 | AI cost измеряется с Sprint 1 | LOCKED | A1 §2 | ai_calls обязателен, billing позже |
| DEC-010 | Self approval явно маркируется | LOCKED | A1 §6.2 | Не выдавать за independent review |
| DEC-011 | StudioStandard версионируется | LOCKED | A1 §6.1 | standard_drift event, no silent rewrite |
| DEC-012 | Публичные роли: Заказчик, Дизайнер, Архитектор, ГлавПрораб | LOCKED | Charter | `site_manager` только technical id |
| DEC-013 | `* Intelligence` только внутренние названия | LOCKED | A1 §4 | Не использовать на сайте/UI |
| DEC-014 | Integration order не является разрешением | LOCKED | A1 §5 | PII-heavy integrations blocked by 152-ФЗ gate |
| DEC-015 | Sprint 1 не закрывает полный Pilot Slice | LOCKED | Charter §14 | Отчёт показывает N/10 |
| DEC-016 | Процентные readiness scores отменены | LOCKED | A1 §7.4 | Использовать status + evidence level |
| DEC-017 | Новые документы не называются Source of Truth | LOCKED | Charter hierarchy | Классы документов фиксированы |
| DEC-018 | Architecture freeze после Canonical Package | LOCKED | Owner command | Изменения через Addendum/Decision Log |
| DEC-019 | Публичный бренд RemHaOS; основной домен `remhaos.com` | LOCKED | OWNER DECISION 28.07.2026 / Addendum A3 | Актуальные UI, docs, metadata и public URLs используют RemHaOS; архитектура не меняется |
| DEC-020 | Brand positioning: `RemHaOS — операционная система полного цикла ремонта: от первого брифа до финальной приёмки` | LOCKED | OWNER DECISION 30.07.2026 | Это публичное позиционирование и категория продукта; Sprint 1 implementation scope не расширяется за пределы Platform Foundation + M1 |
| DEC-021 | M2 открыт; собственный геометрический редактор в scope; ниша — доказуемая история решений, не паритет с CAD; обмен: чтение DWG серверно как подложка, запись DXF, без платных лицензий | LOCKED | OWNER DECISION 08.08.2026 / Addendum A4 | Charter §16 правится по A4 §1.1 (оба бренда); матрица M2 → Build per A4, P1; WF-M2-001 → AUTHORIZED BY A4; значения с подложки неподтверждённые до явного подтверждения (A4 §3.3) |
| DEC-022 | Канонизация подписи версий заморожена: кодовые точки, локалезависимые сравнения запрещены | LOCKED | ADR-001 (+ независимый ADR-0007: паритет с C-collation PostgreSQL) | Изменение канонизации — только новым ADR; контрольный хеш закреплён тестом canonical-signature.test.ts |
| DEC-023 | Позиционирование v2 принято как внутреннее: категория «система записи истины ремонта»; редактор публично — «Планировки», слово CAD наружу не используется; деньги — платит студия за рабочее пространство, клиент бесплатно, без платы за места и экспорт; критерии сдачи M2 дополнены (§8); поза по чатам — «впитываем, не воюем» (§5.1) | LOCKED | OWNER DECISION 08.08.2026 / docs/positioning/REMHAOS_POSITIONING_V2_2026-08-08.md | Публичная подача — по лестнице (столп публикуется после сдачи его кода); чат-мост — отдельным решением после порта и сдачи M2 |
| DEC-024 | M3 открыт в объёме P0 по плану исполнения: intake PDF/JPG/PNG/CSV/XLSX, связи room/sheet/specification, ревизии, completeness/conflict review, baseline, immutable Released Production Package; native CAD/BIM authoring не строится; генерация листов из подписанной версии — пост-P0, отдельным решением | LOCKED | OWNER DECISION 09.08.2026 / Addendum A5 | Матрица M3 → `Build per A5`, P2 (оба бренда); WF-M3-001 → `AUTHORIZED BY A5` (оба бренда); вход — только persisted exact handoff `publish_m2_m3_handoff` / `M2ToM3Handoff`, обходов нет; M4 остаётся закрытым; цикл 7 остаётся открытым и красным |
| DEC-025 | M4 открыт **только в объёме инкремента 1**: `distribute_release`, `acknowledge_release`, `create_change`. Инкремент 2 — `review_change_impact`, `upload_photo_evidence`, `review_photo_evidence`, `accept_milestone`, `build_handover` — НЕ открыт и требует отдельного решения о воркерном контуре; WBS, schedule, split estimate, procurement и Change Order закрыты до wedge validation; ERP/склад/бухгалтерия не входят | LOCKED | OWNER DECISION 10.08.2026 / Addendum A6 | Матрица M4 → `Build per A6 — INCREMENT 1 ONLY`, P2, `Gap` → `Thin P0 in code no UI surface` (оба бренда); WF-M4-001 → `PARTIALLY AUTHORIZED BY A6 — INCREMENT 1 ONLY` (оба бренда); вход — только подтверждённая выдача Released Production Package, обходов нет; старт кода по гейту 1 (`publish_baseline → publish_release` через браузер), доказательство инкремента по гейту 2 с подтверждением сессией получателя; флаг модуля выключен по умолчанию и заведён отдельным guardrail-решением, не этим аддендумом; `M4_PRODUCTION_ENABLED` — отдельное решение и не раньше снятия `PLATFORM_FOUNDATION = BLOCKED`; цикл 7 остаётся открытым и красным |
| DEC-026 | **PROPOSED, не LOCKED.** Приём файлов в M3 P0 строится полностью: продукт хранит сами байты, а не только записи о них. Лимит 100 МБ на файл (конфигурируемый); срок хранения — пока существует проект плюс 90 дней после удаления; антивирусная проверка обязательна в P0, до статуса CLEAN файл в карантине и серверно недоступен для скачивания и невключаем в baseline; контрольную сумму считает сервер, а не клиент | PROPOSED | OWNER DECISION 11.08.2026 / `REMHAOS_OWNER_DECISION_M3_P0_FILES_AND_CONFLICTS_2026-08-11.md` | Не LOCKED: бизнес-модель хранения (§1.6 — черновики в Google Drive, проверенная неизменяемая копия при передаче на проверку, хранение внутри Project Pass / Studio / оплаченного архива, 90 дней read-only и экспорта после окончания оплаты, постоянного бесплатного реального проекта нет, приглашённые не платят) требует подтверждения владельца. Замещает обещание A5 §4.2.4 «вечной читаемости»: опубликованные байты неизменяемы и доступны весь установленный срок хранения проекта. Серверная контрольная сумма — предпосылка DEC-027 |
| DEC-027 | Конфликт документов определяет сервер, а не флаг клиента: совпали логический документ и ревизия, разошлись SHA-256. Одинаковый SHA — дубликат, новая ревизия — новая версия, не конфликт. Неразрешённый конфликт блокирует `publish_baseline`. Разрешает архитектор или владелец проекта по отдельной capability, с обязательным указанием причины; выбранная и отклонённые версии, актор, время и обоснование сохраняются неизменяемо; данные не перезаписываются и не удаляются выборочно в течение установленного срока хранения | LOCKED | OWNER DECISION 11.08.2026 / `REMHAOS_OWNER_DECISION_M3_P0_FILES_AND_CONFLICTS_2026-08-11.md` | `semanticConflict` уходит из входа `register_source`; правило блокировки подключается к пути публикации A′, а не к устаревшей ветке; реализуется после DEC-026 (зависит от серверной контрольной суммы); M3 P0 завершён только после файлов, конфликтов и финального DoD |
| DEC-028 | Уточнение A5 §4.2.2 для встроенного модуля: отдельный маршрут M3 не создаётся; общий workspace остаётся `200`, потому что содержит M2; при выключенном модуле встроенные авторские поверхности и данные M3 отсутствуют в ответе сервера (команды отклоняются до RPC, `operations.*` = `module_disabled`, `documentation` = `null`); будущие собственные URL модуля отвечают `404` | LOCKED (§1–§3) | OWNER DECISION 11.08.2026 / `REMHAOS_A5_CLARIFICATION_M3_ROUTES_2026-08-11.md` | Комментарий в коде требование подписанного документа не замещает — форма действия A5 §4.2.2 задана этим уточнением. §4 (доступность ранее выпущенных пакетов только для чтения при выключенном модуле) — открытый вопрос, требует отдельного решения и согласования с DEC-026 |
| DEC-029 | Механизм выдачи прав инкремента 1 модуля 4: постоянные миграции НЕ возвращают роли `authenticated` права на пишущие RPC M4; `tests/ap1/environment/enable-m4-increment-1.sql` разрешён только для локальных, CI и других одноразовых непроизводственных сред; скрипт не является production-флагом и не является механизмом entitlement; production-включение требует отдельного OWNER GO и аудируемого DB-state; инкремент 2 остаётся закрытым независимо от флага | LOCKED | OWNER DECISION 11.08.2026 / `REMHAOS_A6_CLARIFICATION_M4_GRANT_MECHANISM_2026-08-11.md` | Уточняет A6 §5.2 п.8 ТОЛЬКО в части механизма; подписанный аддендум не переписан, рядом с пунктом стоит ссылка на уточнение. Требование точечности сохранено и проверяется кодом (`PROJECTCEO_M4_INCREMENT_2_LEAKED`). Закрыта находка: guardrail `20260810070000` не закрывал четыре командные RPC модуля 4 в продуктовой схеме — выдача и подтверждение получения были доступны через Data API при выключенном модуле (`20260811020000`). Гейт 2 взят прогоном AP5 195 (`7bf00fd`): состояние `M4_BROWSER_PROVEN`, `M4_PRODUCTION_ENABLED` не наступил |
| DEC-030 | Открыт этап **M4 Worker Foundation / Increment 1.5** в объёме ОДНОГО системного воркера — Release Artifact Worker. Impact calculation, milestones, photo processing и handover НЕ разрешены; M4 Increment 2 не начинать | LOCKED | OWNER DECISION 11.08.2026 | Основание — находка гейта 2: `distribute_release` опирается на артефакт выпуска, который собирает только система (`build_release_artifact`, права у `service_role`), то есть утверждение A6 §1.1 «у инкремента 1 нет воркерных предпосылок» неверно для выдачи. Воркер обязан: сам находить выпущенные версии без артефакта; переиспользовать `buildReleaseDescriptor()` и `ProjectBrainWorkerPostgresAdapter.buildReleaseArtifact()`; работать только системной identity; иметь детерминированные `artifactId` и ключ идемпотентности; переживать повтор, параллельный запуск и потерю ответа; НЕ создавать человеческого HTTP/RPC-доступа и НЕ открывать M4 в production. Доказательство этапа: `authenticated` не вызывает воркер; два параллельных запуска дают один артефакт; повтор после перезапуска не создаёт дубль; пустая очередь — безопасный no-op; AP5 использует настоящий runner вместо тестового `psql`-моста. Итоговое состояние — `M4_RELEASE_WORKER_PROVEN` |
| DEC-031 | Telegram Chat Bridge зафиксирован как ОДИН горизонтальный адаптер `Integration Gateway → Messaging` (не Module 5 и не бот на модуль). Telegram — основной внешний канал переписки; RemHaOS остаётся системой официальных решений, полномочий, версий, изменений и доказательств. P0: один активный чат на проект и один проект на чат, постоянный приём после явного подключения, первый доменный адаптер M3 → M4, обратные уведомления и защищённые переходы. Сообщения, фото, документы и выводы AI создают ТОЛЬКО неподтверждённых кандидатов в Project Inbox; Telegram не переносит полномочия (членство в группе не создаёт роль, «получил» не выполняет `acknowledge_release`, фото не является приёмкой, callback и deep link не выполняют mutation). Бот не создаёт группу — человек создаёт или выбирает её и добавляет бота через `startgroup`; текст действия в UI: «Создать или подключить Telegram-чат». Мост выключен по умолчанию (`REMHAOS_TELEGRAM_BRIDGE_ENABLED=false`) | LOCKED | OWNER DECISION 11.08.2026 / Addendum A7 | A7 меняет Charter §12 ровно настолько, чтобы разрешить разработку и тестирование в local, CI и закрытом staging с тестовыми участниками. НЕ разрешает: production-включение, массовый приём переписки третьих лиц, обход гейта российского data plane, закрытие вопроса 152-ФЗ, M4 Increment 2. Production — отдельный OWNER GO после legal/data-plane/consent/retention gate. Внутренний мессенджер не строится. M1, M2 и прочие сценарии M3 в P0 не подключаются — отдельное решение после первого пилота M3 → M4. Гипотеза `CHAT_INGESTION_HYPOTHESIS_2026-08-08.md` помечена `PARTIALLY SUPERSEDED`: сняты предположения «приём только явным упоминанием бота» и «изменения схемы не нужны» |
| DEC-032 | Целевой человеческий контракт M4 расширяется с 8 до 10 команд: добавляются `define_milestone` и `register_handover_document`; `build_handover` уточняется как человеческий запрос НЕОБРАТИМОЙ финализации, порождающий durable build request. `calculate_change_impact` и `build_construction_handover` остаются worker-only и в число десяти человеческих команд НЕ входят. Подписанный A6 не переписывается. К реализации авторизован ТОЛЬКО объём E0R (восстановление харнесса DB5 и его ввод обязательным заданием CI). V1 Impact, V2 Field Evidence и V3 Handover — **NOT AUTHORIZED** до отдельного **M4 IMPLEMENTATION GO**. Production остаётся закрытым | LOCKED | OWNER DECISION 11.08.2026 «M4 REVISE + E0R GO» и FINAL GOVERNANCE CORRECTION 12.08.2026 / `REMHAOS_A6_CLARIFICATION_M4_EXECUTION_LAYER_2026-08-12.md` | Уточняет A6 §1.1 ТОЛЬКО в части состава контракта и разграничения «целевая архитектура ≠ разрешение на реализацию»; §5.1 (тест, роняющий CI при неучтённой команде) сохраняется и сверяется с числом 10 — подгонять ожидание без ссылки на уточнение запрещено. Основание: `define_milestone` и `register_handover_document` — человеческие RPC с capability `review_milestone` (`20260717103000`), воркером не вызываются, а без них цепочка не замыкается (`WARRANTY_DOCUMENT_REQUIRED`). DEC-025 (ChangeOrder), DEC-029 (механизм грантов), DEC-030 (Release Artifact Worker) и DEC-031 не изменяются; объём модуля не является полным change-control cycle. Исполнено в PR #91: E0R, миграции не изменены |
| DEC-033 | Вертикаль **M4 V1 Impact** разрешена к открытию в production: роли `authenticated` возвращаются РОВНО три двери — `review_change_impact`, `replay_review_change_impact`, `acknowledge_impact_truncation`. Инкремент 1, V2, V3, воркерный контур расчёта и публикация baseline модуля 3 остаются закрытыми; `anon` не получает ничего. Механизм включения — не строка `grant` в миграции, а операция `projectceo_m4.open_v1_impact_production(actor, basis)` с обязательными подписью и основанием и записью в `projectceo_m4.production_switch_log`; откат — `close_v1_impact_production(actor, reason)`. Применение миграции модуль НЕ открывает: оно отзывает права V1 и падает, если после отзыва хоть одна дверь доступна | ПРИНЯТО, ИСПОЛНЕНО ЧАСТИЧНО | OWNER DECISION 12.08.2026 / `REMHAOS_OWNER_DECISION_M4_V1_PRODUCTION_2026-08-12.md` | Это то самое «отдельное решение владельца», которого требовала DEC-029, и вместе с миграцией `20260812030000` — тот самый «аудируемый DB-state»; DEC-029 не переписана, её механизм для одноразовых сред остаётся в силе. **Выключатель построен и доказан прогоном (`tests/db5/28_v1_production_switch.sql`, PG16/17), но НЕ включён.** Не сняты три предпосылки: `PLATFORM_FOUNDATION = BLOCKED` (A6 §7, DEC-025) — требует отдельной записи владельца; вход вертикали в production недостижим (`enroll_organization_project` без HTTP-маршрута, публикация baseline и `submit_change_request` закрыты) — требует либо расширения GO, либо записанного операционного порядка; нет доступа к производственному проекту Supabase. `M4_PRODUCTION_ENABLED` НЕ наступил. Завершённой объявляется вертикаль V1 Impact, а не модуль 4, не Telegram и не RemHaOS |
| DEC-034 | Коррекция контракта покрытия V1 Impact поверх PR #94 (`20260812020000`), поперёк того, что тот PR открыл тем же GO, что DEC-033. `blocked_result_limit` (>5000 найденных влияний) — durable terminal, НИЧЕГО не сохраняется: `returnedImpactCount=0`, `knownImpactCountLowerBound=maxImpacts+1`; первые 5000 НЕ показываются как частичный результат ни при каком раскладе — PR #94 делал ровно это (truncate-and-keep с ручным подтверждением) и этим противоречил DEC-033. `acknowledge_impact_truncation` (дверь ручного подтверждения неполноты, введённая PR #94 тем же GO, что `review_change_impact`) закрыта НАВСЕГДА — human override усечённого прогона в V1 не существует ни при каком состоянии переключателя, включая открытое (`open_v1_impact_production`). Разделение полноты ревью: `allReturnedImpactsReviewed` (все ВОЗВРАЩЁННЫЕ карточки рассмотрены — вакуумно true и для нуля карточек) / `coverageComplete` (`coverageStatus='complete'`) / `impactReviewComplete = allReturnedImpactsReviewed AND coverageComplete` — без подтверждения, без исключений. Сырая `calculate_change_impact` (производный аргумент `max_depth`) закрыта и для `service_role`: единственная системная дверь расчёта — `calculate_change_impact_policy_bound` | LOCKED | OWNER CONTINUE «ЗАВЕРШИТЬ СУЩЕСТВУЮЩИЙ V1» 12.08.2026, поверх DEC-033; ратифицировано явным последующим решением `REMHAOS_OWNER_DECISION_M4_V1_COVERAGE_AND_RECOVERY_2026-08-17.md` §1, отменяющим формулировку DEC-033 §1 «ровно три двери» | Коррекция, а не новая реализация: аддитивная миграция `20260813010000` поверх неизменяемых `20260812010000/020000/030000` — `create or replace function` на `calculate_change_impact`, `review_change_impact`, `get_execution_delivery`, `projectceo_m4._v1_impact_signatures()` (список дверей вертикали сократился с трёх до двух — `review_change_impact`/`replay_review_change_impact`, единая точка для открытия И закрытия переключателя), плюс явные `revoke` на обе закрытые сырые двери и guard-блок, проверяющий всё на живой базе. Старые колонки (`is_truncated`, `truncation_reason`, `calculated_depth`, `policy_max_depth`) не удалены — новые (`coverage_status`, `cutoff_reason`, `has_more_beyond_depth`, `known_impact_count_lower_bound`, `returned_impact_count`, `policy_version`, `max_impacts`) добавлены рядом, инвариант трёх исходов закреплён CHECK-констрейнтом на таблице. Уже доказанная функциональность PR #94 (обход, digest/replay, параллельный воркер, restart/replay, production-выключатель с журналом) сохранена без переписывания — только семантика исходов и дверей исправлена там, где она расходилась с DEC-033. Доказательство: `tests/db5/29_impact_coverage_dec034.sql` (точная граница 5000 не блокирует / 5001 блокирует терминально, приоритет лимита на совмещённом срезе без зонда глубины, исчезновение из очереди воркера, персистентность на таблицах) — оба на PostgreSQL 16 и 17; `26_impact_policy_benchmark.sql` и `28_v1_production_switch.sql` скорректированы под новую семантику (регрессия на реальном широком графе, партиальный прогон без ручного подтверждения, третья дверь недостижима ни при каком состоянии переключателя); AP5 звено 12 скорректировано (`allReturnedImpactsReviewed`/`coverageComplete`/`impactReviewComplete`, попытка вызвать закрытую дверь отклоняется `operation_unavailable` до обращения к базе). DEC-025, DEC-029, DEC-030, DEC-031, DEC-032 не изменяются; V2/V3 не расширяются этим решением; production-выключатель DEC-033 не включён этим решением |
| DEC-035 | Коррекция числа политики обхода V1 Impact: активная политика — `maxDepth = 7`, `maxImpacts = 5000`, версия `project-ceo-impact-policy/0.2`. Исходный OWNER GO зафиксировал `maxDepth = 7`; слитая параллельной сессией миграция `20260812010000` записала `8` — числовая ошибка переноса решения в код, не переоткрытие вопроса. Уже сохранённые (на момент коррекции их не существовало) прогоны остаются подписаны прежней версией (`0.1`/`maxDepth = 8`) — колонки `impact_runs` снимок политики на момент расчёта, а не производная от текущего состояния функции | LOCKED | OWNER REVIEW 12.08.2026, поверх DEC-034; число `maxDepth = 7` явно зафиксировано владельцем 17.08.2026 (`REMHAOS_OWNER_DECISION_M4_V1_COVERAGE_AND_RECOVERY_2026-08-17.md` §2) — археология сессионного GO больше не является основанием | Аддитивная миграция `20260813020000` поверх неизменяемых `20260812010000/020000/030000` и поверх `20260813010000` (DEC-034, тоже неизменяема тем же правилом с момента ревью): ровно `create or replace function projectceo_m4._impact_policy()`, новая версия + `maxDepth = 7`, `maxImpacts` без изменений. `CREATE OR REPLACE FUNCTION` сохраняет OID/владельца/права — грант/revoke `20260812010000` повторять не пришлось. Обёртка `calculate_change_impact_policy_bound` (`20260812020000`) читает `_impact_policy()` заново при каждом вызове — новая глубина действует немедленно, без изменения самой обёртки. Согласно `docs/canonical/remhaos-v1/REMHAOS_MIGRATION_ERRATA.md` (правило вверху файла): ошибка ТЕКСТА миграции лечится записью в errata, ошибка ПОВЕДЕНИЯ — новой миграцией; это ошибка поведения. Доказательство: guard-блок миграции проверяет форму живой политики и её права; `tests/db5/20_execution_operations.sql`, `26_impact_policy_benchmark.sql`, `29_impact_coverage_dec034.sql` читают `_impact_policy()` динамически, а не зашитым числом, поэтому подтверждают коррекцию без переписывания фикстур графов. DEC-033/034 не изменяются — контракт покрытия и закрытие `acknowledge_impact_truncation` те же, меняется только число |
| DEC-036 | Durable надёжность воркера расчёта влияния V1 Impact: ограниченный повтор с растущей паузой (`max_attempts = 5`, `least(2^attempt * 1s, 5m)`), durable dead-letter (постоянный отказ — немедленно; исчерпанный бюджет попыток — после пятой), операторский редрайв (только SQL-действие в приватной схеме, недостижимо ни одной ролью включая `service_role`), исключение `dead_letter`/не наступившего `retrying` из активной очереди воркера. `calculateOne` в `runner.ts` стала тотальной функцией: отказ по ОДНОЙ заявке классифицируется (транзиентный/постоянный), записывается durable через новую дверь и возвращается как исход (`failed_retrying`/`failed_dead_letter`/`unresolved`), а не бросается исключением — один сломанный элемент очереди больше не останавливает обработку остальных и не проваливается молча | LOCKED | OWNER REVIEW 12.08.2026, поверх DEC-034/035 | Аддитивная миграция `20260813030000` поверх неизменяемых `20260812010000/020000/030000` и `20260813010000/020000`: новая приватная таблица `projectceo_m4.impact_worker_failures` (RLS+force, revoke-all+internal-owner policy — та же форма, что у остальных приватных таблиц схемы) с CHECK-констрейнтом формы состояния (`retrying` ⇒ `next_attempt_at` не null и `dead_lettered_at` null, `dead_letter` — наоборот); новая дверь `projectceo_m4_api.record_change_impact_worker_failure` (только `service_role`, атомарный upsert по PRIMARY KEY, `#variable_conflict use_variable` избегается через `p_`-префикс параметров — без префикса он подменяет имена столбцов внутри `ON CONFLICT` target list и ломает разрешение конфликта); новая операторская функция `projectceo_m4.redrive_change_impact_worker_failure` (та же форма, что `open_v1_impact_production`/DEC-033: приватная схема, `revoke all` от всех ролей, обязательные actor/reason, работает только на `dead_letter`-строках); `calculate_change_impact_policy_bound` и `list_change_impact_backlog` (`create or replace`) — успех снимает след прежних отказов, очередь исключает `dead_letter` и не наступивший `retrying`. Доказательство: `tests/db5/31_impact_worker_reliability.sql` (свежая заявка в очереди; транзиентный отказ копит попытки с растущей, но ограниченной паузой; не наступившая пауза временно исключает заявку из очереди; наступившая — возвращает; исчерпание бюджета → dead-letter, ядовитый элемент исчезает из очереди СОВСЕМ, не на паузу; постоянный отказ уходит в dead-letter немедленно, минуя бюджет; редрайв недостижим ни одной ролью; редрайв на не-`dead_letter` строку отклоняется; настоящий редрайв сбрасывает бюджет и возвращает в очередь; повторный редрайв отклоняется; успех снимает след) на PG16 и PG17; настоящая параллельная гонка двух процессов на запись отказа (`run-concurrency.zsh`) даёт одну запись с двумя попытками, не две записи; юнит-тесты `change-impact-worker.test.ts` (транзиентный→retry, исчерпание→DLQ, неразрешимый baseline→durable отказ без бесконечного бэклога, недостижимость записи отказа не роняет проход, состояние попыток отражает базу, а не память процесса, структурный отчёт содержит исход каждой заявки прохода, поражённый элемент не блокирует следующий). DEC-025, DEC-029–035 не изменяются; V2/V3 и production-выключатель не затрагиваются этим решением |
| DEC-037 | Recovery заблокированного прогона V1 Impact: (1) ранняя остановка расчёта — исход `blocked_result_limit` устанавливается ограниченным счётчиком пар (`limit maxImpacts + 1`) ДО материализации результата, дорогая сборка путей рёбер для заблокированного прогона не выполняется; (2) durable alert — строка прогона + явный признак заблокированной заявки в поверхности чтения с рекомендацией rescope + отдельное поле в структурном отчёте прохода воркера; (3) rescope — человеческий путь recovery: новая заявка (`submit_change_request`) с узким набором корней либо против новой baseline, новых человеческих дверей нет; (4) superseding recalculation — системный путь: пересчёт разрешён РОВНО когда единственный активный прогон заявки — `blocked_result_limit` устаревшей версии политики; новый прогон вытесняет старый (`superseded_at`, `superseded_by_impact_run_id`), «один активный прогон на заявку» закрепляется частичным уникальным индексом `where superseded_at is null` вместо безусловного `m4_impact_runs_request_key`; прогоны `complete`/`partial_depth` не вытесняются никогда; человек расчёт не заказывает; (5) ключ идемпотентности воркера включает версию политики | LOCKED | OWNER DECISION 17.08.2026 / `REMHAOS_OWNER_DECISION_M4_V1_COVERAGE_AND_RECOVERY_2026-08-17.md` §3 | Миграция `20260817010000` поверх неизменяемых `20260812*`/`20260813020000`/`20260813030000`; несмерженная `20260813010000` по §5 того же решения приведена к безопасности на непустой `impact_runs` (backfill трёх форм до `NOT NULL`, JSON legacy-прогонов не удаляется, поверхность ведёт себя по счётчикам контракта); доказательства: харнесс апгрейда населённой базы по образцу telegram-апгрейда в CI, DB5-сценарий recovery (заблокированный прогон при той же политике не пересчитывается и очередь пуста; при новой политике заявка возвращается в очередь, новый прогон вытесняет старый, активный ровно один; параллельный пересчёт даёт один активный прогон), юнит-тесты воркера. Production-выключатель, V2/V3, инкремент 1 production и DEC-025/029–036 не затрагиваются |
| DEC-038 | `PLATFORM_FOUNDATION` снят в объёме disposable-пилота: `BLOCKED` → `ACTIVE_FOR_DISPOSABLE_PILOT`. Разделены два смысла флага: запрет на production adoption legacy baseline остаётся (acceptance gate `AP1_MIGRATION_PATH_DECISION_2026-08-01.md`), запрет на платформенный фундамент в репозитории снимается | LOCKED | OWNER DECISION 24.08.2026 / `REMHAOS_OWNER_DECISION_PLATFORM_FOUNDATION_2026-08-24.md`; предпосылки §2 подтверждены `REMHAOS_AP1_PILOT_EVIDENCE_2026-08-24.md` | Разрешает Фазу 2 (project_facts, ai_calls, реестры, Approval gate) и работу на disposable-стендах. НЕ открывает: production adoption, `M4_PRODUCTION_ENABLED`, V2/V3, DEC-026/027, `PRODUCTION_READY=true` |
| DEC-039 | M4 V2/V3 разрешены только для локальной и disposable AP1/AP6 проверки через отдельный environment gate `REMHAOS_M4_V2_V3_ENABLED`; production adoption и `M4_PRODUCTION_ENABLED` не открываются | LOCKED | OWNER DECISION 27.08.2026 / `REMHAOS_OWNER_DECISION_M4_V2_V3_DISPOSABLE_GO_2026-08-27.md` | Разрешены только шесть authenticated human doors: upload/replay photo evidence, review/replay photo evidence, accept/replay milestone. Worker-only и закрытые операции, production Supabase, Integration Gateway и RemHaOS 1 migrations не затрагиваются |
