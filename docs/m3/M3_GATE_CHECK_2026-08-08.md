# M3 · Проверка гейтов старта и план этапов 0–1

Дата: 08.08.2026 · Сессия: контур M3, ветка `claude/new-session-j82fk1`
Исполнен протокол первого часа стартового документа
(`docs/m3/M3_NEW_SESSION_HANDOFF_2026-08-08.md`, разделы 9–10 и гейты раздела 1).

## 1. Топология репозитория — проверено по факту, не по снимку

Расхождения со снимком handoff (он же от 08.08) есть, и они существенны:

| Факт | Снимок handoff | Проверено 08.08 (эта сессия) |
|---|---|---|
| Дефолтная ветка GitHub | `claude/new-session-gsayp3` | так и есть |
| PR #62 (миграции → main) | ждёт слияния | **ВЛИТ** в `main` (`97caad2`) |
| PR #66 (M2-контракты) | ждёт слияния | **ВЛИТ** в `main` (`22fbb24`) |
| Движок в `main` | нет | **частично**: 12 из 28 файлов `lib/layout-studio` (контракты из #66; без экспорта, печати, schema-registry, svg/three-адаптеров и фичефлага) |
| Канон в `main` | нет | по-прежнему нет; ветка `docs/canonical-a4` существует, открытого PR на неё **нет** |
| PR #67 | АРХИВ, не мержить | открыт, draft, в заголовке «НЕ МЕРЖИТЬ» — статус-кво |
| Ветка этой сессии | — | `claude/new-session-j82fk1` = голова `gsayp3` (`8cc44a4`): полный движок, канон, A4, handoff |

Вывод: фаза 2 (порт движка) продвинулась (#62/#66 влиты), но **не завершена** —
в `main` частичный движок и нет канона.

## 2. Гейты раздела 1 handoff

| Гейт | Статус | Факты |
|---|---|---|
| 1. Аддендум A5 подписан | ⛔ | A5 не существовал. Нумерация проверена по файлам: A1–A4 заняты, A5 свободен. **Проект A5 собран этой сессией** из impact-записки: `docs/canonical/remhaos-v1/REMHAOS_ADDENDUM_A5_M3_OPENING.md` — ожидает подписи владельца. Механические правки (журнал, матрица, каталог) НЕ применены — по образцу A4 исполняются тем же коммитом, что и подпись |
| 2. M2 сдан, `DESIGN_FREEZE` существует (versionId+semanticHash) | ⛔ | `DESIGN_FREEZE` в коде отсутствует вообще (grep по репозиторию: только документация). Gap-анализ M2 (`docs/product-intelligence/layout-studio-mvp/MODULE_2_GAP_ANALYSIS.md`) подтверждает: «выход модуля не реализован», закрыто ~2,5 позиции из 10. **Дефект/незавершённость возвращается в M2** — по handoff в M3 не обходится |
| 3. Движок портирован, канон влит | ⛔ | См. §1: порт частичный, канона в `main` нет, дефолтная ветка не переключена |
| 4. Гейты кода зелёные | ✅ | На `claude/new-session-j82fk1`: lint PASS · typecheck PASS · **тесты 447/447** (89 файлов) · production build PASS |

**Итог: код M3 стартовать нельзя.** Разрешённая работа выполнена: проверка,
проект A5, план. Дальше — слово владельца.

## 3. Сверка инвентаря M2 (handoff §4) на этой ветке

Всё из таблицы инвентаря дожило и на месте:

- `lib/layout-studio/` — 28 файлов: adapters (http, local, supabase, svg,
  three), application (editor-session, export-service, print-summary,
  wall-drawing, layout-repository-port), domain (canonical, schema-registry,
  validate, diff, derive, commands, схема v0.1), `feature-flag.ts`;
- миграция `20260808050000_layout_studio_documents.sql` — RLS по образцу
  `0008` (`private.is_studio_member` + `for all to authenticated`) — проверено
  по тексту миграции;
- тесты `tests/layout-studio/` — 27 файлов, в общем зелёном прогоне 447;
- доказательства: `DB_STORAGE_VERIFICATION_2026-08-08.md`, реестр приёмки,
  браузерные прогоны — в `docs/product-intelligence/layout-studio-mvp/implementation/`.

## 4. Проверка «не строить дважды» (handoff §6, этап 1)

Печать в масштабе с рамкой и штампом в M2 **НЕ сделана**: `print-summary.ts` —
печатная сводка (versionId, semanticHash, warnings, таблица ведомости), без
масштаба, рамки и штампа. Этап 1 — реальная работа M3, дублирования нет.
Фундамент готовый: `buildSchedule` (ведомость), SVG-проекция
(`adapters/svg/svg-projection.ts`), привязка каждого экспорта к versionId+хешу.

## 5. План этапов 0–1 — на согласование владельцу ДО кода

### Этап 0 · A5 (документы, кода нет)

1. Владелец читает проект A5 → «Подписываю» (или правки).
2. Тем же коммитом: статус ПОДПИСАН, DEC-023 в журнал, матрица M3 →
   `Build per A5, P2`, WF-M3-001 → `AUTHORIZED BY A5`.
3. Первый коммит кода (разрешён A5 §4 до выполнения входных условий):
   фичефлаг M3 по образцу `ARCHIDOM_LAYOUT_STUDIO_ENABLED` (выключен по
   умолчанию) + каркас маршрутов с 404 при выключенном флаге + тест на 404.
   Функциональности не содержит.

### Этап 1 · Лист из модели (после снятия блокеров §2)

Цель: первый `DrawingSheet` — план в масштабе 1:50/1:100 на печатном листе
с рамкой и штампом, построенный из подписанной версии.

1. Доменная модель листа: формат (A4/A3), масштаб, вид (план), источник —
   строго (documentId, versionId, semanticHash); документ листа — через
   `schema-registry` (новый контракт `*.drawing-sheet/0.1`), подпись — по
   ADR-001.
2. Рендер: существующая SVG-проекция + рамка, основная надпись (штамп:
   проект, лист, масштаб, версия V𝑛, хеш, дата утверждения), масштабная
   верность при печати (`@media print`, мм-координаты уже канонические).
3. Происхождение на листе — человекочитаемо: «построен из версии V3,
   хеш `ef57…`, утверждена клиентом [дата]» + предупреждения версии на листе
   (инвариант 10).
4. Тесты: пин происхождения (лист не собирается без versionId+hash),
   масштабная математика, попадание warnings в лист, 404 при выключенном флаге.

Блокеры этапа 1 (не мои, возвращаются по принадлежности):
- **M2:** реализовать `DESIGN_FREEZE` со ссылкой versionId+semanticHash (A5 §4.1);
- **владелец/контуры:** завершить фазу 2 — порт полного движка и слияние
  канона в целевую ветку (A5 §4.2); нужно слово владельца, куда: `main`
  (докатить #66 до полного движка + влить `docs/canonical-a4`) или явно
  объявить целевой веткой текущую дефолтную `gsayp3`.

### Вопросы владельцу

1. **Подпись A5** — прочитать проект, ответить «Подписываю» или дать правки.
2. **Дефект M2 (`DESIGN_FREEZE`)** — подтвердить возврат в контур M2
   (сессия M3 по handoff его не строит; это выход M2, вход M3).
3. **Целевая ветка** — `main` или `gsayp3`? От этого зависит, где считается
   выполненным гейт 3 и куда портируется движок.
4. После подписи A5 — можно ли делать первый коммит каркаса (флаг + 404)?

## 6. Открытые наблюдения вне моего контура (не трогал)

- PR #65 (docs reference package) — открыт, draft, база `gsayp3`.
- PR #59/#61/#63/#64 — контуры M1/SEO/AP1, к M3 отношения не имеют.
- Живой `.dwg` для проверки A4 §3.2 — по-прежнему не получен (блокер подложки,
  не M3-этапов 0–2).
