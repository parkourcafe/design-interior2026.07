# Skill Contract V1

The internal M1 registry is implemented in `lib/platform/contracts.ts`.
Each of the eight authorized actions declares version, Zod schemas, roles, reads,
writes, audit events, cost class and approval requirement. It has no UI,
marketplace or dynamic arbitrary writes.

