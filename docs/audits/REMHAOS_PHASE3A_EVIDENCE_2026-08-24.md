# Фаза 3a — отчёт с доказательствами (24.08.2026)

Исполнитель: Поток 3 (третий параллельный чат) по ТЗ Фазы 3
(`docs/audits/REMHAOS_LAUNCH_PLAN_2026-08-23.md`, Фаза 3). **PR #102.**
Ветка — `claude/new-session-mlse14` от свежего `main` (616ef02, после мерджа PR #101
Потока 1). Словарь достоверности — по DEC-016, проценты запрещены.

## 0. Входные гейты — как проверялись

- **Этап 3a**: `main` зелёный (последний мердж — PR #101) — стартован. VERIFIED.
- **Этап 3b**: СТОП по обоим условиям ТЗ §0. (1) Ратификации DEC-026 §1.6 в
  `docs/canonical/remhaos-v1/` НЕТ: шапка
  `REMHAOS_OWNER_DECISION_M3_P0_FILES_AND_CONFLICTS_2026-08-11.md:5-8` прямо
  говорит «DEC-026 — НЕ LOCKED… §1.6 требует отдельного подтверждения
  владельца», ни одного файла-ратификации в каталоге нет. (2) PR-3 Фазы 2
  (реестр действий) не смержен: в открытых PR только #100/#101 Потока 1,
  в истории `main` реестра нет. **Ни строчки кода 3b не написано.**
  VERIFIED (листинг каталога, список PR, история).
- **Координация трёх потоков**: коллизий по файлам нет — Поток 1 в этой фазе
  трогал `tests/ap1/environment/*` и docs (PR #100/#101), Поток 2 активных
  веток с командным слоем не имеет; таймстемпы миграций `202608240*0000`
  никем не заняты (`git branch -r` + листинги веток). Файлы чужих зон §1.1 не
  тронуты ни одним коммитом этой ветки (в т.ч. `command-contract.ts`,
  `command-service.ts`, `live-read-port.ts`, `m3-surface.ts`, `m4-surface.ts`,
  `components/projectceo/*`, `app/api/intake/*`, `lib/brief/*`, `lib/llm/*`,
  `tests/ap1/environment/*` — кроме union-дописей в общий
  `migration-ledger.sha256`, что §1.1 прямо разрешает). VERIFIED (`git diff
  --name-only origin/main`).

## 1. Что построено (по пунктам ТЗ §4)

### 3a.4 — авторитетный DB-state включения модулей (M3 №2/№8 предпосылка + M4 №3)
- Миграция `supabase/migrations/20260825010000_projectceo_platform_module_switch.sql`:
  схема `projectceo_platform`, журнал `module_switch_log` (actor/basis
  обязательны, порядок по счётчику `entry_no`), операции
  `open_module_production` / `close_module_production` /
  `module_production_state` / `is_module_open` для модулей `m3` и
  `m4_increment_1`. Один механизм на оба модуля — по образцу
  production-выключателя V1 (`20260812030000`), но параметризованный, а не
  скопированный. Применение миграции принудительно закрывает оба модуля;
  открыть/закрыть может только прямой доступ к базе.
- Доказательства: `tests/db4/49_platform_module_switch.sql` — журнал пуст
  после миграции; выключатель недостижим прикладными ролями (перечисление
  прав + живой 42501-пробинг); циклы close/open фактически переключают права
  и пишут журнал; закрытие/открытие инкремента 1 не задевает ни V1 Impact,
  ни инкремент 2. Гипотеза §5.3 ТЗ — закрыта. VERIFIED (DB4 локально,
  PG16; PG16+PG17 — CI матрицей на PR).
- Механизм НЕ включён ни в одной среде — журнал в production пуст, включение
  остаётся решением владельца (ТЗ §6).

### 3a.1 — read-only проекция released archive (M3 №4)
- `20260825020000_projectceo_released_archive_read.sql`:
  `projectceo_read_api.get_released_archive_read_v1` — версии
  производственных пакетов, артефакты, листы выпущенных пакетов; DTO без
  приватных имён отношений, `logical_content`, `semantic_content`, исходных
  имён файлов и signed URL; доступ по `view_project`; листы — только
  `owner_lead`/`architect` (правило v7). При выключенном модуле — P1113
  `module_disabled`. Там же — `projectceo_platform.m3_read_gate_open()`:
  признак «читающая поверхность M3 открыта» = фактический грант M3-only
  двери (одна физическая граница для выключателя и enable-скрипта).
- Доказательства: `tests/db4/50_released_archive_read.sql` — владелец видит
  архив; `client_approver` (view_project без студийной роли) не видит листов;
  чужая организация — P1103; закрытый модуль — P1113; санитизация DTO;
  сцепка артефакт→версия. VERIFIED.

### 3a.2 — SQL-дверь baseline одной транзакцией + четыре теста (M3 №6+№7)
- `20260825030000_projectceo_publish_baseline_door.sql`:
  `projectceo_product_api.publish_baseline_atomic` — авторизация → replay →
  `publish_version` → серверная деривация состава → `publish_project_baseline`
  → запись двери в общий леджер, всё одной транзакцией. Гонка «новая команда
  при осиротевшей версии» устранена классом. Состав выводится СЕРВЕРОМ:
  активные пакеты, одобренные approval-пакеты, победившая ревизия на
  сущность (зеркало `baseline-composition.ts:145-190`), подтверждённые
  ревизии источников из свежесозданной версии графа — т.е.
  `sourceRevisionIds` входят в composition и semantic hash (вторая половина
  №5). Порядок внутри оставляет место гейту конфликта DEC-027 (после
  replay, до записей) — требование A′-пути (DOC-08 аудита) соблюдено
  структурно, сам гейт — этап 3b.3. Дверь закрыта по умолчанию и входит в
  список включения выключателя; таймаут 30s как у двери публикации версии.
- Доказательства: `tests/db4/51_publish_baseline_door.sql` — (a) повтор
  commandId → прежний успех, вторая версия графа НЕ создаётся (гипотеза
  §5.1), state_revision не движется; (b) новый commandId со старым снапшотом
  → P1006 stale, ничего не записано; (c) инъецированный сбой после доменных
  записей второго шага (`projectceo.product_test_fail_after_domain`) — в
  базе ничего: ни версии, ни baseline, ни записей обоих леджеров;
  (d) потеря ответа после commit → реплей возвращает прежний результат, в
  т.ч. ПОСЛЕ РЕСТАРТА базы (`tests/db4/30_restart_replay.sql`, аргументы
  восстановлены из леджера). Плюс: подтверждённый источник
  `revision-source-1` реально входит в состав опубликованного baseline.
  Перепись RPC-схемы (`tests/db4/10_schema_security.sql`) расширена до 23.
  VERIFIED.

### 3a.3 — воркерный `ingest_source_graph` (M3 №5)
- `20260825040000_projectceo_source_ingest_worker.sql`: очередь
  `list_source_ingest_backlog` (только materialized-инвентарь без графа,
  минус dead-letter/бэкофф; DTO без sanitized_name и ключей планировки);
  системная дверь `ingest_source_graph_system` — ВЕСЬ граф синтезируется
  сервером из строки инвентаря, воркер передаёт только идентификаторы;
  исполнение — от имени зарегистрировавшего автора
  (транзакционно-локальный request-claim, обоснование в шапке миграции;
  произвольная имперсонация невозможна — автор выводится сервером из строки
  инвентаря, вызвать может только service_role); durable-контур отказов —
  зеркало DEC-036 (бэкофф `least(2^n сек, 5 мин)`, max 5 попыток,
  dead-letter, операторский redrive только прямым доступом к базе, успех
  стирает след).
- Воркер: `lib/project-intelligence/workers/ingest-source-graph/`
  (planner+runner, тотальный `ingestOne`, перенос state_revision вперёд по
  строкам одного проекта — несколько источников проходят одним проходом),
  адаптер `adapters/postgres/ingest-worker.ts`, скрипт
  `scripts/run-ingest-source-graph-worker.ts`
  (`npm run worker:ingest-source-graph`; ненулевой код возврата при
  dead-letter). В CI воркер не запускается — только стендовые доказательства
  (ТЗ 3a.3).
- Доказательства: `tests/db4/52_source_ingest_worker.sql` — границы ролей;
  очередь самосогласованна и отдаёт ровно инвентарь-без-графа (фикстура
  сценария 37); контур отказов целиком; системный ингест с проверкой
  актора (`source_ingestions.actor_user_id` = регистрировавший) и
  системного провенанса (`created_by_id='system:projectceo-ingestion'`);
  реплей; P1109 на дубль; **разрыв сценария 37 закрыт**: v8 предлагает
  ревью, `review_source` проходит, `human_reviews` записан.
  `tests/projectceo-integration/ingest-source-graph-worker.test.ts` — 13
  юнит-тестов (детерминизм ключа, гонки §5.2 ТЗ: повтор — replay, дубль —
  успех гонки; отказ одного источника не роняет проход; недостижимая запись
  отказа не роняет проход; ломаный конверт роняет весь проход). VERIFIED.

### M3 №2 — гейт читающих RPC
- `20260825050000_projectceo_m3_read_gate.sql`: при выключенном M3 Data API
  не отдаёт source/sheet данные. Базовое чтение правится хирургией
  `pg_get_functiondef` (приём `20260801120000` — тело в базе уже не равно
  тексту миграции после auth-rewrite; копия реинтродуцировала бы
  `auth.uid()` и уронила `DB4_MANAGED_AUTH_REFERENCE_REMAINS`): `sources` →
  `[]`, `sourceStats` → нули. v7 пересоздан с гейтом до веток ролей — ключи
  листов не ставятся. Гейт действует на ВСЕХ версиях цепочки (они
  оборачивают одну базу). TS-файлы command-слоя не тронуты (§1.3 ТЗ).
- Доказательства: `tests/db4/53_m3_read_gate.sql` — open: данные есть;
  close: sources пуст, счётчики нули, ключей листов нет, поверхность M1/M2
  (решения, согласования) не задета, гейт виден и на v7; reopen: данные
  вернулись. Guard миграции: гейт вшит в оба места, managed-auth не
  реинтродуцирован, грант v7 не потерян. VERIFIED.

### 3a.5 — сведение двух дверей выдачи/подтверждения (M4 №2)
- Порядок строго по backlog: СНАЧАЛА покрытие перенесено на request-bound
  двери (`tests/db4/20_product_operations.sql`: выдача и подтверждение —
  request-bound; P1103 чужой получатель; P1107 чужой хеш СО СВЕЖИМ КЛЮЧОМ —
  у request-bound хеш входит в request digest, повтор ключа дал бы P1108
  раньше проверки; НОВОЕ покрытие P1109 RECIPIENT_SCOPE_REQUIRED — его не
  было нигде), ЗАТЕМ `20260825060000` заменяет тела
  `distribute_release`/`acknowledge_release` безусловным отказом
  `LEGACY_DOOR_RETIRED_USE_REQUEST_BOUND`. Гипотеза §5.4 ТЗ выполнена в
  форме «дверь мертва при любом гранте»: guard миграции зовёт двери от
  владельца (максимальные права) и требует отказа; db4/20 пробует их живым
  вызовом из-под `authenticated` в среде, где enable-скрипт их грантует.
  Уточнение к формулировке ТЗ («403/404 через Data API»): физическое
  снятие грантов/сигнатур упирается в чужие зоны — см. §3 (координация);
  выбранная форма даёт то же свойство (вызов не исполняет семантику
  никогда) без единой правки чужих файлов.
- VERIFIED (DB4 и DB5 — db4/20 гоняется в обоих).

### 3a.6 — не предлагать публикацию при устаревшей ревизии (M4 №5, SQL-половина)
- `20260825070000_projectceo_workspace_read_v10_superseded_approvals.sql`:
  v10 оборачивает v9 (конвенция версий) и добавляет
  `data.approvalSupersededEntities` — сущности, чья победившая одобренная
  ревизия уже не текущая в графе и текущая не переодобрена. Непустой список
  — сигнал поверхности отдавать `publish_baseline` как
  `prerequisite_missing` (A6 §4.2.5).
- Доказательства: `tests/db4/54_workspace_read_superseded_approvals.sql` —
  управляемая фикстура: одобрили r1 → сущность НЕ флагуется; пересмотрели в
  r2 без переодобрения → флаг с точными полями
  (targetKind/entityId/approvedRevisionId/currentRevisionId); v9 поля не
  имеет; самосогласованность с независимым предикатом по базе. VERIFIED.
- TS-половина — запрос координации (§3): без правки `live-read-port.ts`
  поверхность поле не потребляет; для requirement/assumption сущностей v10
  — единственный источник текущих ревизий в проекции.

### 3a.7 — чтение executor-скриптов (закрытие −2 баллов M2)
- Отчёт `docs/product-intelligence/M2_EXECUTOR_SCRIPTS_REVIEW_2026-08-24.md`
  (docs-only коммит): все 4 shell-исполняемых файла построчно, allowlist
  (digest-сверка VERIFIED), 9 TS-модулей, 6 тестов. 7 дефектов (2 major).
- Отдельным коммитом (не docs) исправлены оба major: D1 — харвест не писал
  `actorUserId`/`actorSessionId`, билдер детерминированно отклонял каждый
  реальный прогон (`EXTERNAL_RUN_COMMAND_ACTOR_UNBOUND`); D2 — чеканка UUID
  была Linux-only (`mint_uuid()` c fallback на `uuidgen`). Digest раннера в
  `allowlist.json` обновлён; 60 тестов pilot-evidence зелёные. Дефекты
  D3–D7 (minor) названы в отчёте и НЕ чинились — отдельное решение.
- Готовность AP6 (3b.4): протокол приёма построен полностью; после фикса
  D1 главный детерминированный блокер снят; конвертер «пакет партнёра →
  манифест», загрузка данных пакета в одноразовый стек и cookie-jar'ы пяти
  ролей — NOT_BUILT, ждут владельца/партнёра. Прогон
  `external-package-runner.zsh` на синтетическом пакете в этой среде
  невозможен (нужен живой HTTP-стек приложения + Supabase-стек; egress
  стенда не позволяет поднять полный стек) — DOC_TARGET до одноразовой
  среды, ТЗ относит его к 3b.4.

## 2. Гипотезы ТЗ §5 — статус

1. Повтор `publish_baseline` с тем же commandId не создаёт вторую версию —
   ЗАКРЫТА (db4/51 Т2, счётчик версий). VERIFIED.
2. Воркер: повтор после потери ответа — replay; параллельные — один
   результат (гонки закрыты replay/P1109, юнит-тесты + db4/52 §5); отказ
   одного источника не роняет проход (юнит «keeps working past a refusal»
   -аналог: отказ classифицируется, проход продолжается). VERIFIED.
3. Механизм 3a.4: применение миграции НЕ включает (журнал пуст + guard
   close_on_apply); open/close недостижимы прикладным ролям (включая
   service_role); журнал пишет actor/basis. VERIFIED.
4. Legacy-двери: семантика P1103/P1107 живёт на request-bound (db4/20 +
   ap1/reads/20 — там же P1108/актор-привязка); прежние двери отвечают
   отказом при любом гранте. Форма «403/404» заменена на «тело-отказ» —
   причина в §1 (3a.5) и §3. VERIFIED в выбранной форме.
5. Новые RPC: чужая организация → P1103 (db4/50, db4/51 наследует от
   внутренних дверей, db4/52 — контур только service_role); выключенный
   модуль → module_disabled (db4/50 P1113; поверхность — m3-surface
   offState); PII в логах/DTO нет (санитизация проверяется db4/50 и
   db4/52 явными LIKE-пробами). VERIFIED.
6. `auth-regression` (vitest, в составе `npm test`) и
   `DB4_MANAGED_AUTH_REFERENCE_REMAINS` (db4/10) — зелёные после всех
   миграций. VERIFIED.

## 3. Запросы координации (§1.2 ТЗ — решает владелец)

1. **M3 №8, финальный флип** (Поток 2, после его PR-3): перевести
   `command-service.ts` (publish_baseline) на `publish_baseline_atomic`,
   затем удалить сырые `publish_version`/`publish_project_baseline`/
   `publish_production_package_version` из `_module_signatures('m3')` и
   добавить строку двери в матрицу `m3-surface.ts`. До перевода приложения
   сырые RPC в списке включения оставлены НАМЕРЕННО (иначе включение модуля
   открыло бы неработающий продукт) — названо в самой миграции.
2. **M4 №5, TS-половина** (Поток 2): потребление
   `approvalSupersededEntities` в `live-read-port.ts:1257-1264`
   (`publish_baseline` → `prerequisite_missing` при непустом списке) +
   переключение версии RPC в `authenticated-read.ts:838` на v10.
3. **M4 №2, физическое удаление** (Потоки 1 и 2): убрать выведенные из
   строя сигнатуры из `m4-surface.ts` (и связанные count-сверки
   `m4-surface-matrix.test.ts`) и из `enable-m4-increment-1.sql`; после
   этого можно DROP функций отдельной миграцией.
4. **M4 №6 — НЕ закрыт Потоком 2** (проверено 24.08.2026):
   `command-service.ts:978` всё ещё перечисляет
   `owner_lead`/`architect` вместо вывода из `_role_capabilities`. Файл в
   зоне Потока 2 — этому потоку недоступен.

## 4. Что ждёт владельца (§7 ТЗ)

1. Ратификация DEC-026 §1.6 — гейт всего этапа 3b (3b.1 хранение байтов,
   3b.2 антивирус, 3b.3 конфликты, полноценный 3b.4).
2. Выбор hosted AV-сервиса (адаптер — после выбора).
3. Реальный внешний пакет + манифест для цикла 7 (закрытие цикла 7 вне
   этого ТЗ; синтетикой закрывать запрещено тестом).
4. Решение о включении модулей через механизм 3a.4 — только после приёмки;
   этот PR ничего не включает нигде.
5. Мердж PR; разрешение запросов координации §3.

## 5. Гейт и среда доказательств

- Полный гейт на финальном SHA: `npm run lint` (0 ошибок; 5 предсуществующих
  warning в чужом коде), `npm run typecheck`, `npm run test`
  (161 файл / 1392 теста), `npm run build`, `npm run test:db4`,
  `npm run test:db5` — зелёные. `git status` чистый.
- **Отступление среды, названное честно**: docker-реестр закрыт
  egress-политикой стенда (403 на CONNECT к production.cloudfront.docker.com
  — policy denial, обходить нельзя). DB4/DB5 прогнаны локально на нативном
  PostgreSQL 16.13 через шим docker-команды (initdb/pg_ctl/psql вместо
  контейнера; сам `run.zsh` исполнялся без изменений, включая
  concurrency-скрипты, оба upgrade-подсценария на собственных кластерах и
  restart-replay). PG17 локально недоступен (нет пакета в noble) —
  доказательство PG16+PG17 даст обязательная CI-матрица на PR
  (`.github/workflows/ci.yml:121,158`). Уровень: локально VERIFIED (PG16);
  CI-прогон Actions run 32718049782→32718104856 на HEAD `ca88ca4` ЗЕЛЁНЫЙ:
  DB4 и DB5 на postgres:16-alpine И postgres:17-alpine, AP5 authenticated
  browser matrix, lint/typecheck/test/build — CI_EVIDENCED. Единственный
  красный чек — «cycle 7 evidence (informational)»: намеренно красный гейт
  до передачи владельцем манифеста реального пакета, красный и на main,
  мерж не блокирует.
- Ledger миграций: пять строк добавлено (union-дисциплина, только свои
  файлы), `sha256sum --check` зелёный. Перепись миграций Layout Studio
  (`integration.test.ts`) дополнена шестью своими строками с комментариями.
- Production Supabase `ztnycrchwxqczqbyegnp` не тронут ни одним запросом;
  V2/V3 не открывались; инкремент 2 не начинался; подписанные документы не
  переписывались; probe/force-push/мерджей чужих PR — ноль.

## 6. DoD этапа 3a (ТЗ §9) — сверка

1. Шесть открытых пунктов M3-backlog: №2 ✅, №4 ✅, №5 ✅, №6 ✅, №7 ✅ —
   кодом с тестами; №8 — закрыт по существу + строка координации (§3.1). ✅
2. M4: №2 ✅ (по существу + координация на физическое удаление), №3 ✅,
   №5 ✅ (SQL-половина; TS — координация §3.2); №6 — проверено: Потоком 2
   НЕ закрыт (§3.4). ✅ (в форме honest-строки)
3. Конфликтов, прожатых силой, — ноль: ветка от свежего main, ledger —
   только union-записи. ✅
4. Полный гейт на финальном SHA зелёный (§5); PG17 — CI на PR. ✅/CI
5. Этот отчёт. ✅
