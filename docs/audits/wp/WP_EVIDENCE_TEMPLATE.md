# WP-xx — <название> — EVIDENCE

Дата: YYYY-MM-DD. Ветка: `wp/WP-xx-<slug>`. HEAD: `<sha>`. PR: #N. Сессия: `<тег/идентификатор>`.

## Основание
[ИЗВЛЕЧЕНО] трек/шаг дорожной карты; решения владельца (Р-номера, DEC-номера), на которые опирается пакет.

## Allowlist по факту
[ИЗВЛЕЧЕНО] вывод `git diff --name-only origin/main...HEAD` и подтверждение, что каждый файл входит в
allowlist карточки.

## Хотспоты
[ИЗВЛЕЧЕНО] каких H-хотспотов коснулся и почему это разрешено карточкой; подтверждение «остальные не тронуты».

## Пины
| пин (файл:строка) | было | стало | основание (документ/DEC) |
|---|---|---|---|
(или «пинов нет»)

## Миграция
timestamp | строка ledger | DB4/DB5-сценарий | S-MIG # (или «нет»)

## Локальные гейты
[ИЗВЛЕЧЕНО] команды, коды выхода, версии node/npm; `release:check`; DB4/DB5, если SQL задет.

## CI
[ИЗВЛЕЧЕНО] run id каждой джобы на точном HEAD; артефакт AP5; классификация re-run, если был.

## Grep-проверки
[ИЗВЛЕЧЕНО] что искали / команда / что нашли — для шагов, где карта ссылается на код.

## Не сделано / вынесено
[ИНТЕРПРЕТИРОВАНО] что не вошло и почему; предложенный follow-up WP.

## Blind review
сессия | дата | вердикт | замечания и их закрытие (SHA)

## Безопасность
[ИЗВЛЕЧЕНО] production не использовался; секретов в логах нет; production-ref в промпте и evidence нет.
