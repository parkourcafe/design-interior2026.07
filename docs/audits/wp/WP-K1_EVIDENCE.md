# WP-K1 — актуализация операционных документов — EVIDENCE

Первичный пакет: 2026-09-11. Проверка перед merge: 2026-09-12.
Ветка: `wp/wp-k1-handoff-launch-checklist-refresh`. PR: #149.
Исправления ниже подготовлены поверх `fd7b0fe`; текущий commit определяется
по HEAD этого PR.

## Основание

[ИЗВЛЕЧЕНО] WP-K1 покрывает дорожную карту 0.13 (К-10, К-11, К-13) и обновление операционных документов по аудиту 08.09.2026.

[ИЗВЛЕЧЕНО] Прямое решение владельца: «Р7 принимаю по рекомендации, Р20 выбираю вариант (а), Р22 — платный M1→M2 цикл для дизайнера».

[ИНТЕРПРЕТИРОВАНО] Эти решения зафиксированы как контекст программы; WP-K1 не реализует ни один из них и не меняет security, enrollment или коммерческий цикл.

## Allowlist по факту

[ИЗВЛЕЧЕНО] `git diff --name-only origin/main` содержит только `HANDOFF.md`, `LAUNCH_CHECKLIST.md`, `REMHAOS_CONFLICT_REGISTER_2026-08-02.csv`, `HANDOFF_CINEMATIC.md`, `supabase/email-templates/README.md` и этот evidence-файл.

## Хотспоты

[ИЗВЛЕЧЕНО] H9 и H10 не затронуты.

## Пины

[ИЗВЛЕЧЕНО] Пинов нет.

## Миграция

[ИЗВЛЕЧЕНО] Миграций и DB4/DB5-сценариев нет.

## Локальные гейты первоначального пакета 11 сентября

[ИЗВЛЕЧЕНО] `npm ci --cache /private/tmp/npm-cache-wpk1` завершился с кодом 0 после того, как обычный `npm ci` остановился на root-owned общем npm cache; источник и lockfile не менялись.

[ИЗВЛЕЧЕНО] `npm run release:check` завершился с кодом 0: lint без errors (13 существующих warnings), typecheck, 205 test files / 1642 tests и build прошли на Node v22.23.0 / npm 10.9.8.

[ИЗВЛЕЧЕНО] `git diff --check` завершился с кодом 0.

## CI

[ИЗВЛЕЧЕНО] Первичный CI на `fd7b0fe` прошёл (run `34563865090`).
Эта проверка не подтверждает последующие исправления. Новая версия требует
собственного CI. Claude Max сообщил об исчерпанном лимите; новый Claude job
`34702243491` завершился с ошибкой, содержательное review не подтверждено.

## Grep-проверки

[ИЗВЛЕЧЕНО] Прежние имена намеренно сохранены в историческом cinematic-снимке
и в `supabase/email-templates/README.md`, где «Свод» обозначает незавершённый
ребрендинг HTML и запрет их текущей production-установки. Это не заявка на
полное удаление прежнего бренда из репозитория.

## Не сделано / вынесено

[ИНТЕРПРЕТИРОВАНО] В первоначальном пакете 11 сентября shared DB, SMTP, DNS,
production branch, deploy и feature flags не проверялись и не изменялись.
12 сентября read-only проверка Vercel UI подтвердила Production Branch Tracking
`release` и выключенное автоматическое назначение production-доменов.
Настройки не изменялись; разрешение на merge не распространяется на deploy,
shared DB или эти внешние настройки.

## Blind review

[ИЗВЛЕЧЕНО] Для docs-only WP обязательный security blind review не требуется.

При проверке перед merge исправлены описание одного общего продукта M1–M4,
границы pre-sale M1, неподтверждённые текущие метрики/ограничения среды и
инструкции установки писем со старым брендом. Исторический launch checklist
явно отделён от текущих acceptance/adoption gates. HTML и runtime не менялись.

Независимое review исправленной документации подтвердило основные изменения;
неточности исторического checklist и evidence устранены. Повторный локальный
прогон 12 сентября: lint без errors (13 прежних warnings), typecheck, все тесты
и `npm run build -- --webpack` — PASS. Это локальное доказательство; новый CI
и недоступный Claude review остаются отдельными проверками.

## Безопасность

[ИЗВЛЕЧЕНО] Production, shared DB, credentials, CI configuration и deploy не использовались.
