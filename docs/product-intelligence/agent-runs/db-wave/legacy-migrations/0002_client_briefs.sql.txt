-- ─────────────────────────────────────────────────────────────
-- Client-initiated briefs (вариант 2): клиент сам заполняет бриф без
-- привязки к дизайнеру и получает публичную ссылку-бриф, которую сам
-- рассылает выбранным дизайнерам. Никакого каталога/матчинга дизайнеров —
-- это НЕ маркетплейс.
--
-- Единственное изменение: projects.designer_id становится необязательным
-- (клиентский бриф не принадлежит дизайнеру). Доступ по-прежнему только по
-- токену через server route с service role; anon-политик нет.
-- ─────────────────────────────────────────────────────────────

alter table public.projects alter column designer_id drop not null;
