# M4 Production Hardening — backlog

**Статус:** заведён 11.08.2026 вместе с постройкой инкремента 1.
**Правило то же, что у `M3_PRODUCTION_HARDENING_BACKLOG.md`:** каждый пункт
**обязателен до внешнего production-пилота M4** и **не блокирует** дальнейшую
разработку. Пункт закрывается кодом с тестом, не галочкой.

| # | Что | Почему отложено |
|---|---|---|
| ~~1~~ | ~~**Воркер артефакта выпуска.**~~ **ЗАКРЫТО 11.08.2026** — этап M4 Worker Foundation / Increment 1.5 (DEC-030), отчёт `M4_INCREMENT_1_5_WORKER_REPORT_2026-08-11.md`. Остаток пункта: чем запускать воркер в продакшене (cron, очередь, вызов после публикации) — решается вместе с production-включением M4 | — |
| ~~2~~ | ~~**Две двери у выдачи и подтверждения.**~~ **ЗАКРЫТО ПО СУЩЕСТВУ 24.08.2026 (Фаза 3a)** — покрытие семантики (P1103 чужой получатель, P1107 чужой хеш со свежим ключом, НОВОЕ P1109 чужая область) перенесено на request-bound двери (`tests/db4/20_product_operations.sql`), затем тела прежних дверей заменены безусловным отказом LEGACY_DOOR_RETIRED (`20260824060000`) — дверь мертва при любом гранте. Остаток-координация: физическое удаление сигнатур из `m4-surface.ts` (зона Фазы 2) и `enable-m4-increment-1.sql` (зона Фазы 1) | Их семантику (чужой получатель → `P1103`, чужой хеш → `P1107`) проверяет `tests/db4/20_product_operations.sql`. Отозвать их насмерть сегодня значит потерять это покрытие; свести к одной двери — вместе с переносом проверок на request-bound версии |
| ~~3~~ | ~~**Авторитетный DB-state включения модуля**~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — общий механизм для M3 и M4-инкремент-1: `projectceo_platform.module_switch_log` + open/close/state с обязательными actor/basis (`20260824010000`); применение миграции принудительно закрывает модули; тест `tests/db4/49_platform_module_switch.sql` (включая границы поражения V1 и инкремента 2). Включение в любой среде — по-прежнему отдельное решение владельца | Тот же пункт, что у M3 (там он записан как предпосылка backlog). Пока его нет, `enable-m4-increment-1.sql` остаётся механизмом одноразовой среды, и production-включение запрещено |
| 4 | **Покрытие предпосылки приёмки вехи** — три теста в `live-read-sanitization.test.ts` проверяли «приёмка предлагается только после принятия всех фото»; сегодня операция закрыта как инкремент 2, и тесты проверяют закрытость | Возвращается вместе с открытием инкремента 2; фикстуры трёх состояний оставлены в тесте намеренно, чтобы возврат был правкой утверждений, а не написанием заново |
| ~~5~~ | **Поверхность предлагает публикацию baseline, когда одобренная ревизия уже заменена.** **SQL-ПОЛОВИНА ЗАКРЫТА 24.08.2026 (Фаза 3a)** — v10 отдаёт `approvalSupersededEntities` по правилу «одна ревизия на сущность» (`20260824070000`, тест `tests/db4/54_workspace_read_superseded_approvals.sql`); потребление в `live-read-port.ts` (prerequisite_missing) — запрос координации, зона Фазы 2 до её PR-3. Решение пересмотрели, новую ревизию не одобрили — сборщик возьмёт последнюю одобренную, а база откажет: она не входит в текущую версию графа | Найдено вместе с правкой «одна ревизия на сущность» (11.08). Отказ базы честный, но нарушено A6 §4.2.5: действие не должно предлагаться. Лечение — сверка состава с текущими ревизиями графа в чтении; трогать состав, на котором доказан гейт 1, ради несвязанного случая опаснее, чем назвать остаток |
| 6 | **Роли выдачи в приложении выведены, а не перечислены.** Сейчас `command-service.ts` сравнивает `scope.role` со списком `owner_lead` / `architect`; база выводит то же из `_role_capabilities` | Расхождение уже случалось (до 11.08 приложение пропускало только `owner_lead`, а поверхность предлагала выдачу и архитектору). Лечение — общий источник capability для сервера, а не второй список |

**Определение готовности пункта:** миграция/код + DB4-сценарий или юнит-тест,
падающий при регрессе + строка в этом файле переведена в «закрыто» со ссылкой
на PR.
