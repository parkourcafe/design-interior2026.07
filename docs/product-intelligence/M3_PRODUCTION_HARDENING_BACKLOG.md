# M3 Production Hardening — backlog

**Статус:** принят решением владельца 11.08.2026 вместе с закрытием #79.
**Правило:** каждый пункт **обязателен до внешнего production-пилота M3** и
**не блокирует** разработку M4. Пункт закрывается кодом с тестом, не галочкой.

Источник — внешнее ревью #79. Номера сохранены из него, чтобы обсуждения не
расползались по двум нумерациям. Пункты 1 и 3 из того списка выполнены в самом
#79 (отзыв M3-only RPC; утечка package scope + детерминированная метка) и здесь
не повторяются.

| # | Что | Почему отложено |
|---|---|---|
| ~~2~~ | ~~Чтения M3 в матрице; при выключенном M3 прямой Data API не отдаёт draft/source/sheet данные~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — гейт внутри читающих RPC по фактическому гранту M3-only двери: миграция `20260825050000` (хирургия базового чтения + v7), предпосылка — авторитетный DB-state `20260825010000`; тесты `tests/db4/53_m3_read_gate.sql`, `tests/db4/49_platform_module_switch.sql` | Читающие RPC общие с M2 — отозвать нельзя; нужен gate внутри RPC, то есть авторитетный DB-state, который сам вынесен в отдельное решение (см. ниже) |
| ~~4~~ | ~~Отдельная безопасная read-only проекция для released archive~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — `get_released_archive_read_v1`: версии, артефакты, листы выпущенных пакетов, санированный DTO, доступ по view_project, module_disabled при закрытом модуле; миграция `20260825020000`, тест `tests/db4/50_released_archive_read.sql` | Поверхности Project Archive ещё нет; проекция строится вместе с ней. Продуктовая основа — §1.7 решения DEC-026/027 |
| ~~5~~ | ~~`sourceRevisionIds` выводятся сервером из проверенных источников и входят в composition, snapshot token и semantic hash~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — воркерный контур ingest (`20260825040000`: очередь + системная дверь + DEC-036 отказы; воркер `lib/project-intelligence/workers/ingest-source-graph/`) и серверная деривация состава в атомарной двери (`20260825030000`); тесты `tests/db4/52_source_ingest_worker.sql` (после ингеста ревью проходит), `tests/db4/51_publish_baseline_door.sql` (подтверждённый источник входит в baseline) | Клиент уже шлёт только токен, поля уже в токене и хеше — но всегда пусты: источники попадают в граф только через воркерный `ingest_source_graph`, которого нет. Пункт активируется вместе с воркерным контуром |
| ~~6~~ | ~~Создание graph version и baseline — одна транзакционная операция~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — SQL-дверь `publish_baseline_atomic` (`20260825030000`): версия графа и baseline одной транзакцией, осиротевшая версия невозможна классом; место под гейт конфликта DEC-027 оставлено (после replay, до записей) | Детерминированная метка (сделано в #79) уже даёт resume через replay того же `commandId`. Остаток — SQL-дверь одной транзакцией против гонки «новая команда при осиротевшей версии» |
| ~~7~~ | ~~Четыре теста семантики~~ **ЗАКРЫТО 24.08.2026 (Фаза 3a)** — все четыре в `tests/db4/51_publish_baseline_door.sql` + реплей после рестарта базы в `tests/db4/30_restart_replay.sql`; плюс счётчик версий: повтор commandId не создаёт вторую версию графа | Доказывают семантику пункта 6; делать вместе с ним. Частично покрыто: детерминизм метки и stale-token уже под тестами |
| ~~8~~ | При включённом M3 `authenticated` не получает сырые descriptor-RPC; публична только orchestration-дверь. **ЗАКРЫТО ПО СУЩЕСТВУ 24.08.2026 (Фаза 3a), остаток — координация**: composition перенесена в SQL-дверь (`20260825030000`) — частичный baseline прямым вызовом двери невозможен; дверь входит в список включения выключателя. Остаток: удалить сырые publish-RPC из `_module_signatures('m3')` МОЖНО только после перевода command-слоя на дверь (зона Фазы 2 до её PR-3) — иначе включение модуля открыло бы неработающий продукт. Запрос координации в отчёте Фазы 3a | Правило полноты живёт в приложении: прямой вызов с грантом может опубликовать частичный baseline. Лечение — перенос composition в SQL-дверь; сегодня гранты существуют только в одноразовых средах |

**Связанное отдельное решение (не пункт backlog, а его предпосылка):**
production-включение M3 — один авторитетный DB-state с аудитом (кто, когда, на
каком основании), env-флаг поверх него как дополнительный hard-off. Грант из
`enable-m3-publication.sql` — механизм одноразовой среды, production им не
включается. Пункты 2 и 8 технически зависят от этого решения.

**Определение готовности пункта:** миграция/код + DB4-сценарий или юнит-тест,
падающий при регрессе + строка в этом файле переведена в «закрыто» со ссылкой
на PR.
