# KORA coordinate freeze

Status: `BLOCKED_BY_MISSING_AUTHORITATIVE_COORDINATES`

The release evidence freezes rules and dimensions but does not contain the coordinates
required to build an authoritative KORA fixture. The following inputs remain unresolved:

- exact Tenant 12 contour and clear height;
- every door parent wall, offset, width and height;
- column centre X/Y (section is confirmed as 250 × 250 mm);
- sink X/Y/W/D/H;
- EQ-01..EQ-05 positions and verified XLSX dimensions;
- EQ-04 height conflict;
- anchors for the stated 3000 mm distance.

No coordinate is inferred from an AI or perspective image. Until the owner-approved
table exists, the repo uses a clearly labelled synthetic Liquid Station demo fixture.
It validates the full editor, version and export workflow but is not KORA construction
evidence and cannot satisfy LS-AT-010..016.
