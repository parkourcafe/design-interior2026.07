# R1-08A private declared sheet sidecar

One private command binds an existing original-source pair confirmation to an
existing native documentation-sheet revision in the exact package. It derives
source identities/hashes from the immutable pair and rechecks current architect
capability, accepted source readiness and locked write eligibility before replay.
No renderer, preview representation, object/technical reference, UI or release is
created. No runtime grants or module opening are added.

The command stores the declared page/crop/rotation/units/matrix values exactly.
It validates bounds and finite invertibility, but applies no renderer coordinate
convention. Geometry remains architect_declared and page metadata not_verified;
there is no page-existence, MediaBox or visual-alignment claim. The result retains
the mandatory unconfirmed-DWG warning.

The imported pure contract9010ceb is already present through562283f. Its geometry
shape/refinement is extracted and reused; original candidate DTO behavior is kept.
SQL validation separately rejects malformed JSON, invalid page/crop/rotation,
nonfinite/singular or overflowing matrix/inverse/corner arithmetic.

Sidecar, command and audit are atomic with bidirectional deferred closure and
append-only triggers. The private operation registry is extended additively.
Exact historical sidecars remain untouched; replay does not synthesize a current
mapping or accept a new source/sheet revision on an old key. UTC output is stable.

Final source hashes:

- migration: `716de726ac782ce24da4d54c5d98de9a30580da5f2822f42c217b667e235608d`;
- fixture74: `295ce93670130aebf1697ed0b35db6dafebaece7beb80c36266217fef8a67106`.

Local lint passed with 13 existing warnings; typecheck, 220 files / 1,819 tests
and Webpack build passed. Independent source/security review passed after fixing
exact numeric crop bounds before float conversion and adding standalone
command/audit closure negatives.

Root executed a clean socket-only PostgreSQL 16 chain containing the standard
product and M2→M3 sheet fixtures, real synthetic source-pair evidence, sidecar
behavior, both authorization race orders and actual database restart/replay.
All passed; the owned cluster was stopped and removed. Sanitized receipt:
`artifacts/R1_08A_SHEET_SIDECAR_LOCAL_PG16_RECEIPT.json`.

The fixture explicitly proves that the declared values do not create a
representation, technical reference or new sheet. PostgreSQL 17, full CI DB4/
DB5, AP5 and Claude review remain draft-PR gates. No shared database, production,
cloud, deployment, module opening or merge action was performed.
