# R1-08A historical sheet-sidecar exact-ID reader

The new read RPC returns only the immutable declared sidecar projection. Current
view_project and active owner/architect scope are checked with locks and refreshed
after waiting, before exact-ID lookup. Builder/client alone remain denied; exact
package architect is supported. Source readiness/write policy and original signer
employment are not rechecked for historical evidence.

Reader EXECUTE is default revoked and included only in the existing M3 registry;
the existing effective M3 read gate also guards accidental isolated reader grants.
No table/helper exposure, byte access, UI/list/renderer/object/release is added.
The app adapter refuses module-off before RPC and canonicalizes strict UUIDs.
The existing sidecar result schema is extracted and reused without dropping its
full geometry refinement. Declared/not_verified/unconfirmed markers stay literal.

Fixture76 is adapted from the previously verified pair-reader pattern. It uses
persisted74 data, independent exact JSON projection from DB fields, symmetric
history comparison, command/audit count snapshots and an exact workflow snapshot, private access denial,
roles/package-only capability/anonymous gates, source cancellation history, and
embedded two-session revoke-first/read-first overlaps with exact blocking PID.
Modes: r1_sheet_sidecar_read_keep_fixture plus r1_sheet_sidecar_read_overlap;
then actual restart followed by r1_sheet_sidecar_read_restart_check.

Implementer ran 59 focused tests successfully. Typecheck/lint reported separately.
The SQL fixture and embedded overlap were authored but not executed by implementer;
root subsequently ran the native disposable PG16 chain, including fixture76 and
its real overlap/restart modes. No cloud/DB/module activation/commit/push/deploy/
merge is claimed. Disposable enable-script edits are repository changes, not
executed activation. No hosted/browser proof is claimed.
