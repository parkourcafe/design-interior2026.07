# Dependency-security WP — owner-authorized local scope

Owner authorization: «Разрешаю», responding to the explicit request for a separate
local dependency-security WP, independent review/tests and draft PR, without
merge, deploy or production changes. Date:2026-09-23, Asia/Tashkent.
Base:8a27b93a0877be7998ee6037961c64501d78de00; branch
codex/dependency-security-20260923. Independent from WP32 PR208.

## Scope

Remediate the12 dependency advisories observed in the unchanged baseline lockfile.
Primary cases: Next AVIF/Windows advisories, Sharp/libheif, Vitest/mocker; review
transitive xmldom, baseline-browser-mapping, brace-expansion, browserslist,
fast-uri, js-yaml, nanoid and tar. Advisory severity is not proof of deployment
exploitability; production is not inspected or modified.

Allowed edits: package.json, package-lock.json, this work card and evidence file.
Minimal compatibility regression tests may be added when a changed dependency
needs a concrete guard. No product feature, API/schema/Auth/RLS, CI, deployment,
signed canonical document or unrelated configuration changes.
No new dependency family, force upgrade or broad npm audit fix. Review lockfile
churn and preserve unrelated explicit pins/overrides. Escalate incompatible
major updates or required changes outside this boundary instead of guessing.

## Verified package candidates

Registry readback confirmed Next/eslint-config-next16.3.3, Sharp0.35.4 and
Vitest4.1.11 exist. Current Node22.23.0 and React18.3.1 satisfy their reported
engine/peer constraints. This is compatibility preflight, not acceptance.

## Acceptance

1. Independently inspect dependency paths and compatible patch constraints.
2. Produce narrow reviewed manifest/lockfile diff and fresh audit report; preserve
   any unresolved advisory with exact package/path/precondition, never suppress it.
3. Reproducible npm ci; lint, typecheck, full tests and build. Verify framework/Auth
   integration through permitted local disposable browser tests where applicable.
4. Fresh independent read-only bypass/regression review, then relevant rechecks.
5. Commit/push/draft PR only after required checks; recheck CI/Vercel side effects.
   No merge/deploy, production/shared/Drive/Pejeng or paid external calls.

External WP32 source-provenance gate remains open and is not claimed by this WP.
