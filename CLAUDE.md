# CLAUDE.md — AI-квалификация клиента и сборка КП для интерьерных дизайнеров (MVP v0.1)

## Что мы строим

Walking skeleton pre-sale продукта: дизайнер отправляет клиенту ссылку → клиент проходит quick brief (~10 вопросов с ветвлением) → система строит паспорт проекта и карточки рисков → дизайнер на Review Board принимает/отклоняет риски → собирает и отправляет коммерческое предложение.

Это слой ДО дизайна: до чертежей, до визуализаций, до закупок. Цель v0.1 — рабочее демо для платного пилота и WTP-интервью, не production-масштаб.

## Кто пользователь

Платит и настраивает — дизайнер. Рынок русскоязычный: интерфейс на русском, валюта — рубль. Клиент дизайнера проходит бриф по публичной ссылке без регистрации.

## Стек (зафиксирован — не менять и не обсуждать)

- Next.js 14+ App Router, TypeScript strict
- Supabase: Postgres + Auth (magic link по email) + Storage (план/фото)
- LLM продукта: YandexGPT (Yandex Cloud Foundation Models) — основной провайдер по стратегии; GigaChat (Сбер) — задокументированный запасной. Провайдер и модель из env: `LLM_PROVIDER` (yandex|gigachat|zai), `LLM_MODEL`, `YC_FOLDER_ID`, `YC_API_KEY`. Вызовы ТОЛЬКО из server route handlers через `lib/llm/provider.ts`. Anthropic / OpenAI в runtime продукта — нельзя.
  - **Отступление (одобрено владельцем):** фактический runtime продукта — `zai` (Zhipu GLM, `glm-4.6`). Это осознанное отступление от исходного запрета «китайских API». Решение подтверждено 04.07.2026 в т.ч. для холодного трафика Входа Б (Фаза 4): персональные данные незнакомых людей идут через того же провайдера. Митигация — маскирование контактов/города до вызова LLM (`lib/risks/llm.ts`, 152-ФЗ). Смена на YandexGPT не трогает продуктовый код (только env).
- Tailwind CSS; без тяжёлых UI-библиотек
- Vitest для unit-тестов логики
- Деплой: Vercel

## Архитектурные правила

1. Ключи только в env. `.env.example` обязателен, секреты никогда не коммитятся.
2. Любой ответ LLM — строгий JSON по схеме (схема дублируется в тексте промпта). Парсинг через Zod `safeParse`; при провале — один repair-retry («верни только валидный JSON по схеме»), затем фолбэк: UX не падает, показываем rule-карточки.
3. RLS включён: дизайнер видит только свои проекты. Публичный intake работает через server route по `intake_token` (сверка токена на сервере, service role); anon-ключ доступа к чужим данным не даёт.
4. Все строки интерфейса — в `lib/i18n/ru.ts`. Это подготовка будущей EN-локализации, саму EN НЕ делаем.
5. Деньги — integer в рублях, диапазоны как [min, max].
6. LLM-провайдер спрятан за интерфейсом `lib/llm/provider.ts` с единственным методом `completeJSON(prompt, schema)`: смена YandexGPT → GigaChat → западный провайдер при EN-экспансии не трогает продуктовый код. Актуальные имена моделей и эндпоинты сверяются с официальной документацией провайдера в фазе 0 — не по памяти.

## Модель данных

```sql
designers: id uuid (= auth.users.id), name, studio_name,
  pricing jsonb null,           -- null = режим «без цены»
  proposal_defaults jsonb       -- exclusions, лимит правок, условия этапов

projects: id, designer_id, client_name,
  status ('created'|'brief_sent'|'brief_in_progress'|'brief_completed'|'proposal_draft'|'proposal_sent'),
  intake_token text unique, passport jsonb, created_at

answers: id, project_id, question_id text, value jsonb, created_at

risk_cards: id, project_id,
  risk_type ('budget'|'timeline'|'function'|'style'|'technical'),
  evidence text[], impact text, confidence ('low'|'medium'|'high'),
  designer_action text, proposal_implication text,
  status ('proposed'|'accepted'|'rejected'), source ('rule'|'llm')

proposals: id, project_id, version int, sections jsonb,
  status ('draft'|'sent'), public_token text, sent_at

events: id, designer_id, project_id, type text, created_at
```

Файлы клиента — в Supabase Storage; метаданные пишутся в answers (`question_id = 'attachments'`).

### Shadow-паспорт (`projects.passport`)

```
object: { type: 'flat'|'house'|'apartments', area_m2: number, city: string }
asset_horizon: 'self_long' | 'sell_2_5y' | 'rent' | 'unknown'
household: { now: string, in_5y: string, kids: boolean, pets: boolean }
lifestyle: { morning_load: 'low'|'mid'|'high', bathrooms: number,
             cooking: 'none'|'basic'|'heavy', storage_pressure: 'low'|'mid'|'high' }
budget: { range: [number, number] | 'undisclosed', risk_level: 'low'|'mid'|'high' }
timeline: { target: string, urgency: 'normal'|'urgent' }
style: { refs: string[], anti: string[], notes: string }
pain_points: string
scope: { package: 'concept'|'full'|'full_plus_supervision'|null }
```

Паспорт заполняется детерминированно функцией `buildPassport(answers)` — обязательно покрыть unit-тестами.

### Вход Б и shareable-паспорт: добавления к схеме (Фаза 4, из Патча P1 §2)

Строится ТОЛЬКО в Фазе 4 после закрытия гейта. Имена полей выровнены по фактическим миграциям (источник истины — `supabase/migrations/`, не патч).

```sql
projects (добавляется):
  source text not null default 'entry_a' check (source in ('entry_a','entry_b'))
  designer_id  -- УЖЕ nullable (migration 0002); NULL допустим только при source='entry_b' (CHECK)
  portable boolean not null default false     -- entry_b: true с создания; entry_a: → true ТОЛЬКО по INV-B1
  client_contact jsonb                         -- верифицированный контакт (отдельно от passport.contact, который сырой из брифа)
  contact_verified_at timestamptz              -- контакт обязателен и верифицирован до отправки брифа (оба входа)
  transferred_to_pool_at timestamptz
  -- новый терминальный статус:
  status ... check (... 'declined_transferred' ...)   -- явный отказ с передачей в пул
  -- ДОЛГ/уточнение: в текущем enum нет ни 'declined', ни 'declined_transferred'; в Фазе 4 добавить declined_transferred
  --                 (и решить, нужен ли отдельный простой 'declined')

passport_shares (новая):
  id, project_id fk, share_token text unique, status ('active'|'closed'),
  created_at, closed_at
  -- СЕРВЕРНОЕ ограничение: ≤3 записей status='active' на проект (транзакция/триггер, не только UI) — INV-B3

range_confirmations (новая):
  id, project_id fk, designer_id fk (ПРИГЛАШЁННЫЙ дизайнер, не владелец),
  token text, system_range jsonb (снапшот ценового движка), confirmed_range jsonb null,
  status ('sent'|'confirmed'|'declined'|'expired'), sent_at, responded_at
  -- expiry 48 ч после sent_at; создание записи — операционное действие (скрипт/SQL), UI создания НЕ строить
```

**Антифрод (Фаза 4):** rate limit на отправку брифов — 5/IP/сутки (значение в конфиг); верификация контакта 6-значным кодом по email через выделенный SMTP (не встроенная почта Supabase) или по телефону.

## Quick brief: 10 вопросов

Источник истины — `lib/brief/questions.ts`. Каждый вопрос несёт: `id`, текст, тип (choice/multi/number/text/files), `passport_field`, `show_if` (ветвление).

1. `object` — тип объекта, площадь м², город → object.*
2. `asset_horizon` — «Что будет с этим жильём через 3–5 лет?» (для себя надолго / возможно продам / сдам в аренду / пока не знаю) → asset_horizon
3. `household` — кто живёт сейчас; кто может добавиться за 5 лет: дети, родители, животные → household.*
4. `morning` — «Как выглядит будний утренний час?» — сколько человек собираются одновременно + сколько санузлов сейчас → lifestyle.morning_load, lifestyle.bathrooms
5. `cooking` — готовите ли и как часто; ветка `show_if != 'none'`: на скольких человек → lifestyle.cooking
6. `storage` — «Что сейчас не помещается?» (одежда / спорт / техника / книги / всё помещается) → lifestyle.storage_pressure
7. `budget` — бюджетный коридор на ремонт и комплектацию: диапазоны + «не готов назвать» → budget.range
8. `timeline` — когда хотите закончить; флаг срочности → timeline.*
9. `style` — 3–5 референсов (ссылки или загрузка) + что точно НЕ нравится → style.*
10. `pain` — «Что больше всего раздражает в текущем жилье?» — свободный текст → pain_points

Опциональный шаг «загрузить план/фото» — только хранение, БЕЗ анализа изображений.

Продуктовое правило тона: вопросы поведенческие, не прямые. Не «нужна ли вам ванна» — а «как проходит утро». Никаких вопросов про доход: только поведенческие прокси.

## Выявление противоречий: гибрид

**Слой 1 — детерминированные правила** (`lib/risks/rules.ts`, тестируются, `confidence='high'`, `source='rule'`):
- budget низкий/средний + премиальные материалы в style.notes/refs → budget
- asset_horizon = sell/rent + запросы глубокой кастомизации → budget/scope
- timeline.urgency = urgent + индивидуальная мебель в пожеланиях → timeline
- storage_pressure = high + «минимализм» в стиле → function
- morning_load = high + bathrooms = 1 → function

**Слой 2 — LLM-проход**: один серверный вызов с passport + сырыми ответами; промпт требует строго JSON-массив карточек по схеме risk_card; confidence ставит модель. Дедупликация с карточками слоя 1.

**Тон карточек — продуктовое правило:** «Возможный риск: … Evidence: … Рекомендуем обсудить: …». Никогда «клиент противоречит себе». AI предлагает — дизайнер утверждает или отклоняет.

## Цена (v0.1 — просто)

`designers.pricing = { base_rate_per_m2, multipliers: { complexity: {low, mid, high}, urgency, package: {...} } }`.

Диапазон = base × area × (произведение выбранных множителей, min..max). Расчёт детерминированный, разбивка показывается дизайнеру («почему цена такая»). Если `pricing = null` — режим «без цены»: КП собирается без блока стоимости.

НИКАКОГО pricing-мастера с реверс-инжинирингом из прошлых проектов — это v0.2.

## Proposal

Секции: задача клиента (авто из брифа) · состав работ · этапы и сроки · стоимость (если есть) · что входит / что не входит · лимиты правок · что нужно от клиента · условия завершения этапов.

Каждая секция — редактируемый текст, предзаполненный из паспорта, принятых risk-карточек (`proposal_implication` → «что входит/не входит») и `proposal_defaults`. Публичная страница `/p/[public_token]` + версия для печати через `@media print` — это и есть «PDF» v0.1. Кнопка «Отправить» → `status='sent'`, событие `proposal_sent`.

## События (метрики валидации из стратегии)

Писать в `events`: `intake_link_created`, `brief_started`, `brief_completed`, `proposal_created`, `proposal_sent`. Отдельный дашборд не нужен — достаточно данных в таблице.

**Вход Б (Фаза 4, из Патча P1 §5) — добавляются:** `brief_block_completed` (block_id — воронка drop-off по решению 12.1), `contact_verified`, `share_created`, `share_viewed`, `share_closed`, `transferred_to_pool`, `match_requested` (CTA подбора), `range_confirmation_sent` / `range_confirmed` / `range_declined` / `range_expired`.

## Что НЕ строить в v0.1 (guardrails — абсолютные)

Биллинг и тарифы · мультивалютность и EN-локализация · ML-pricing и pricing-мастер · генерация изображений/концепций · анализ загруженных планов · спецификации · deep brief (только quick) · PDF-библиотеки (печатная HTML-страница) · внешние интеграции · Anthropic / OpenAI в runtime продукта. Китайские LLM API — по умолчанию нельзя, кроме задокументированного отступления на `zai` (см. «Стек»). Ограничения не касаются инструментов разработки — Claude Code разрешён.

**Вход Б (маркетплейс) — уточнение после Патча P1.** Тонкий слой Входа Б (лендинг, shareable-паспорт, «Передать в пул», one-tap подтверждение вилки, верификация) переходит из запрета в scope **Фазы 4** — строго по разделу «Вход Б и shareable-паспорт» ниже. По-прежнему НЕ строить (абсолютно, даже если «логично дополнить»): каталог дизайнеров и публичные профили · дашборд/лента пула · уведомления дизайнерам о брифах пула · кредиты/карма/приоритеты/квоты как код (это таблица операционной команды) · алгоритмический матчинг в любом виде · сортировки/сравнения по цене и показ вилки одного дизайнера другому · короткая/усечённая версия брифа · создание `range_confirmations` через UI.

Любая идея из этого списка или вне scope — строка в `BACKLOG.md`, не код.

## Вход Б и shareable-паспорт (Фаза 4 — из Патча P1)

Основание: Amendment A1 к Strategy v2.4 (утверждён) + Патч P1. Статус — Should. **Строится ТОЛЬКО в Фазе 4 после закрытия гейта; до этого — ни строчки кода фичи.** При конфликте P1 с базовым CLAUDE.md побеждает P1 (он новее).

**Контекст: два входа, один движок.**
- Вход А (существующий, неизменен): дизайнер → intake-ссылка `/i/[token]` → бриф → паспорт/риски/КП. Железное правило: клиент Входа А никогда не видит других дизайнеров.
- Вход Б (новый): клиент без дизайнера → публичный лендинг → тот же полный бриф (без сокращений) → паспорт + риски + предварительная вилка как самостоятельная ценность → опционально «Подобрать дизайнера» (создаёт заявку операционной команде; сам подбор — ручной процесс вне кода).

**Экраны и роуты (P1 §4):**
- **S-B1. Лендинг Входа Б** (`/start` или корень публ. домена): та же форма брифа без брендинга дизайнера; страница результата — паспорт + карточки + вилка + CTA «Подобрать дизайнера» (запись заявки + email операционной команде, событие `match_requested`).
- **S-B2. Shareable-паспорт** `/s/[share_token]`: read-only паспорт + карточки + вилка; внизу крючок для дизайнера («Хотите получать такие брифы? Создайте свою ссылку»); счётчик по INV-B4; кнопка «Закрыть доступ» у клиента-владельца.
- **S-B3. «Передать в пул»** на экране отказа дизайнера: диалог → статус `declined_transferred` → `portable=true` (строго INV-B1) → уведомление операционной команде.
- **S-B4. One-tap подтверждение вилки** `/c/[token]`: сводка паспорта + системная вилка + «Подтверждаю, готов пообщаться» + опц. корректировка вилки; после ответа токен гаснет; expiry 48 ч.
- **S-B5. Шаг верификации контакта** внутри intake-потока (оба входа): контакт → 6-значный код → отправка брифа.
- **Автосейв черновика брифа** (полный бриф выживает на холодном трафике; вопросы не сокращать).

**Инварименты (в guardrails, P1 §3):**
- **INV-B1.** `portable=false → true` для entry_a — единственным путём: действие дизайнера-владельца, ставящее `declined_transferred`. Ни один другой код не пишет в это поле. Паспорт entry_a с `portable=false` не рендерится ни на каком публичном URL.
- **INV-B2.** UI клиента entry_a не содержит ни одного элемента Входа Б (ни CTA подбора, ни упоминаний других дизайнеров, ни роутов). Расширение железного правила.
- **INV-B3.** Максимум 3 активных share на проект; 4-я попытка отклоняется СЕРВЕРОМ с понятной ошибкой.
- **INV-B4.** Страница share показывает «паспорт открыт ещё у N дизайнеров» (N = активные share минус текущий) и НИКОГДА — имена/ссылки/идентификаторы других получателей.
- **INV-B5.** Дизайнер никогда не видит вилку другого дизайнера; нигде нет сортировки по цене; подпись вилки везде: «предварительный ориентир, подтверждён дизайнером».
- **INV-B6.** Отправка брифа невозможна без верифицированного контакта (оба входа).
- **INV-B7.** Патч не добавляет новых LLM-вызовов: share — рендер существующего паспорта, вилка — существующий ценовой движок, верификация/события — детерминированный код. (Провайдер — по текущему env, см. отступление на `zai` в «Стек».)

**Операционка (НЕ код).** Подбор дизайнеров, кредиты реципрокности, приоритет донора — ручной процесс операционной команды по отдельному регламенту. Не автоматизировать и не предлагать автоматизацию в `BACKLOG.md` чаще одного раза.

**DoD Фазы 4 (P1 §6):** цикл entry_b без дизайнера (лендинг → верификация → полный бриф с автосейвом → паспорт+карточки+вилка); серверный тест лимита share (4-й отклонён, закрытие освобождает слот); `/s/...` для entry_a при `portable=false` → 404, после `declined_transferred` → 200; `portable` немутабелен вне INV-B1; в UI entry_a нет элементов Входа Б; отправка без верификации невозможна + rate limit; цикл вилки sent→confirmed и sent→expired (48 ч); все новые события пишутся; строки — из `lib/i18n/ru.ts`; диф по коду — новых LLM-вызовов нет.

### Статус гейта Фазы 4 (на 04.07.2026)
- ✅ **DoD Фазы 3** — принят (билд/тесты зелёные, `PHASE_3_REPORT.md`).
- ✅ **Патч P1 влит в CLAUDE.md** — этот раздел.
- ✅ **Runtime-LLM** — решение владельца: остаётся `zai` (холодный трафик Входа Б принят; митигация — маскирование PII).
- ⛔ **Выделенный SMTP для кодов верификации** — НЕ настроен (Resend + домен). Верификация — первый кирпич Фазы 4 (S-B5), поэтому **код Фазы 4 не стартует, пока нет SMTP.**

## Definition of Done для каждой фазы

`npm run build`, `lint`, `typecheck`, `test` — зелёные · ключевая логика фазы покрыта unit-тестами (`buildPassport`, rules, pricing, ветвление) · написан `PHASE_N_REPORT.md`: что сделано / как проверить руками за 5 минут / известные ограничения / вопросы · секреты не в коде.

## Команды

`npm run dev` · `npm run build` · `npm run lint` · `npm run typecheck` · `npm run test`
