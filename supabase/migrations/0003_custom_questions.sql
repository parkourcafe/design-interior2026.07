-- ─────────────────────────────────────────────────────────────
-- Свои вопросы дизайнера: дизайнер добавляет собственные вопросы к брифу
-- конкретного проекта. Они показываются клиенту после стандартных вопросов,
-- ответы попадают в answers и видны дизайнеру.
--
-- Храним как массив строк в projects.custom_questions.
-- ─────────────────────────────────────────────────────────────

alter table public.projects
  add column if not exists custom_questions jsonb not null default '[]'::jsonb;
